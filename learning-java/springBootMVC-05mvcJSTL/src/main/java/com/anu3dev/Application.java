package com.anu3dev;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class Application {
	public static void main(String[] args) {
		SpringApplication.run(Application.class, args);
	}
}

/*
 *
 * JSTL stands for JSP Standard Tag Library. JSTL is the standard tag library that 
 * provides tags to control the JSP page behavior. JSTL tags can be used for iteration 
 * and control statements, internationalization, SQL etc.
 *
 */