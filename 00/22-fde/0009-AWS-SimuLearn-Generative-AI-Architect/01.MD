# Amazon Bedrock AgentCore — Complete Developer Notes

> Practical understanding of Amazon Bedrock AgentCore, Runtime, Gateway, Targets, Lambda, RAG, Knowledge Bases, Secrets Manager, RDS, and application integration.

---

# 📚 Table of Contents

1. [Temperature](#1-temperature)
2. [AgentCore — Big Picture](#2-agentcore--big-picture)
3. [AgentCore Runtime](#3-agentcore-runtime)
4. [AgentCore Gateway](#4-agentcore-gateway)
5. [Gateway Target](#5-gateway-target)
6. [Gateway → Lambda → Secrets Manager → RDS](#6-gateway--lambda--secrets-manager--rds)
7. [Complete Customer Support Example](#7-complete-customer-support-example)
8. [Real-World ABC Shoes Architecture](#8-real-world-abc-shoes-architecture)
9. [Frontend → Node.js → AgentCore](#9-frontend--nodejs--agentcore)
10. [Invoking AgentCore Runtime from Node.js](#10-invoking-agentcore-runtime-from-nodejs)
11. [AgentCore Runtime ARN](#11-agentcore-runtime-arn)
12. [RAG and Knowledge Base](#12-rag-and-knowledge-base)
13. [Knowledge Base vs Gateway](#13-knowledge-base-vs-gateway)
14. [Live Data vs Knowledge Data](#14-live-data-vs-knowledge-data)
15. [Complete Shoe Shopping Flow](#15-complete-shoe-shopping-flow)
16. [Route 53 vs API Gateway vs ALB](#16-route-53-vs-api-gateway-vs-alb)
17. [Complete Production Architecture](#17-complete-production-architecture)
18. [Do We Always Need All These Services?](#18-do-we-always-need-all-these-services)
19. [Security Boundaries](#19-security-boundaries)
20. [Mental Model](#20-mental-model)
21. [Quick Revision](#21-quick-revision)
22. [Interview-Level Summary](#22-interview-level-summary)

---

# 1. Temperature

## What is Temperature?

Temperature controls the **randomness / creativity** of an LLM response.

| Temperature | Behavior | Best For |
|---|---|---|
| Low / close to `0` | Focused, deterministic | Technical answers, factual tasks |
| Medium | Balanced | General conversations |
| High / close to `1` | More varied and creative | Creative writing, brainstorming |

### Easy Memory

```text
LOW temperature  → predictable
HIGH temperature → creative / varied
2. AgentCore — Big Picture

Amazon Bedrock AgentCore provides infrastructure for building and running AI agents.

An Agent can:

Understand the user's request
Decide what information it needs
Retrieve information from a Knowledge Base
Call tools
Access backend capabilities through Gateway
Generate a final natural-language response
Important Concept

An Agent is not just an API.

A traditional API might do:

GET /products

An Agent can decide:

User request
      ↓
What does the user need?
      ↓
Knowledge Base?
      ↓
Tool?
      ↓
Backend data?
      ↓
Generate answer

The Agent decides what it needs to do.

3. AgentCore Runtime
What is AgentCore Runtime?

AgentCore Runtime is where your deployed AI Agent actually runs.

Think:

AgentCore Runtime
        ↓
    Runs Agent
        ↓
Agent understands request
        ↓
Agent decides what to do

Your Node.js or Java application can invoke the deployed AgentCore Runtime.

Runtime Invocation

AWS provides the InvokeAgentRuntime API.

Conceptually:

Node.js / Java
      │
      │ InvokeAgentRuntime
      │
      │ Runtime ARN + user message
      ▼
AgentCore Runtime
      │
      ▼
     Agent

The response can be streamed back to the application.

4. AgentCore Gateway
What is Gateway?

AgentCore Gateway is a tool access / mediation layer.

The Agent uses Gateway when it needs access to external tools or backend capabilities exposed through that Gateway.

Important

Gateway does not need to know your database password.

Gateway does not directly become your database.

Instead:

Agent
  ↓
AgentCore Gateway
  ↓
Target
  ↓
Lambda
  ↓
Backend system
5. Gateway Target
What is a Target?

A Target tells Gateway:

"When this tool/capability is requested, where should the request go?"

Example:

AgentCore Gateway
       │
       └── Target
             │
             └── cs_product_lambda

Suppose the tool is:

check_inventory

Input:

{
  "product_id": "PEGASUS-2026",
  "size": 10
}

Gateway maps this tool capability to the configured target.

Conceptually:

check_inventory
      ↓
Gateway
      ↓
Target
      ↓
cs_product_lambda
6. Gateway → Lambda → Secrets Manager → RDS

This is one of the most important flows to remember.

Complete Flow
Step 1 — Agent Calls Tool

Suppose the user asks:

Where is my order 12345?

The Agent decides:

I need order information.

Available tool:
get_order_details

Tool input:

{
  "order_id": "12345"
}
Step 2 — Agent Calls Gateway
Agent
  ↓
Gateway

Gateway knows:

get_order_details
        ↓
      Target
        ↓
cs_bot_lambda_diy

This relationship was configured when the target was created.

Step 3 — Gateway Invokes Lambda

Lambda receives:

{
  "order_id": "12345"
}
Step 4 — Lambda Gets Database Credentials

Lambda should NOT contain credentials like:

username = "admin"
password = "mypassword123"

Instead Lambda knows the ARN of the secret.

Example:

DB_SECRET_ARN=
arn:aws:secretsmanager:us-east-1:123456:secret:rds-secret

Lambda calls:

Lambda
   ↓
Secrets Manager

Secrets Manager returns:

{
  "username": "dbuser",
  "password": "********",
  "host": "mydb.xxxxx.rds.amazonaws.com",
  "port": 3306
}
🚨 Critical Security Point
Agent ❌ does NOT receive DB credentials

Gateway ❌ does NOT need DB credentials

Lambda ✅ receives credentials

Secrets Manager ✅ stores credentials
Step 5 — Lambda Queries RDS

Lambda connects to RDS and executes something like:

SELECT order_id, status, delivery_date
FROM orders
WHERE order_id = '12345';

RDS returns:

{
  "order_id": "12345",
  "status": "SHIPPED",
  "delivery_date": "2026-09-06"
}
Step 6 — Result Returns to Agent
RDS
 ↓
Lambda
 ↓
Gateway
 ↓
Agent

The Agent receives business data, not the DB credentials.

Step 7 — Agent Generates Human Response

Agent knows:

Order: 12345
Status: SHIPPED
Delivery: September 6

It generates:

Your order 12345 has shipped and is expected to arrive on September 6.

7. Complete Customer Support Example

Let's put everything together.

User
Where is my order 12345?
Complete Flow
8. Real-World ABC Shoes Architecture

Imagine you're working for ABC Shoes.

Existing application:

Frontend:
React / Angular

Backend:
Node.js

Database:
RDS

Infrastructure:
AWS

You want to add:

AI Customer Support / Shopping Agent
Production Architecture
9. Frontend → Node.js → AgentCore
Step 1 — Customer Opens Website

Customer visits:

https://abcshoes.com

React application loads.

Customer asks:

I need running shoes under $100 for women.

Step 2 — React Calls Your Backend

React should normally call your application backend.

POST /api/chat
Content-Type: application/json

Request:

{
  "message": "I need running shoes under $100 for women."
}

Flow:

React
  ↓
POST /api/chat
  ↓
Node.js
10. Where Does /api/chat Come From?

This is important.

/api/chat is your application's API.

It is not automatically created by AgentCore.

You create it in Node.js.

Example:

app.post("/api/chat", async (req, res) => {
    // Call AgentCore Runtime
});

Production example:

Browser
   ↓
https://abcshoes.com
   ↓
React
   ↓
https://api.abcshoes.com/api/chat
   ↓
Node.js

The API domain may be exposed through:

Route 53
   ↓
API Gateway / ALB
   ↓
Node.js

This part belongs to your application's web architecture.

11. Invoking AgentCore Runtime from Node.js

Node.js needs to know:

Where is my deployed AI Agent?

The AgentCore deployment provides a Runtime identity / ARN.

Example:

AGENTCORE_RUNTIME_ARN=
arn:aws:bedrock-agentcore:us-east-1:123456789012:runtime/ShoeAgent-abc123

Also configure:

AWS_REGION=us-east-1
🚨 Never put this configuration in React

React should NOT know:

❌ AgentCore Runtime ARN
❌ AWS credentials
❌ IAM permissions
❌ Gateway details
❌ Lambda ARN
❌ RDS endpoint
❌ Database password

These belong on the server side.

12. Node.js → AgentCore Runtime

Node.js uses the AWS SDK.

Install:

npm install @aws-sdk/client-bedrock-agentcore

Example:

import {
  BedrockAgentCoreClient,
  InvokeAgentRuntimeCommand
} from "@aws-sdk/client-bedrock-agentcore";

import crypto from "crypto";

const client = new BedrockAgentCoreClient({
  region: process.env.AWS_REGION
});

const agentRuntimeArn =
  process.env.AGENTCORE_RUNTIME_ARN;

export async function askAgent(userMessage) {

  const sessionId = crypto.randomUUID();

  const payload = JSON.stringify({
    prompt: userMessage
  });

  const command = new InvokeAgentRuntimeCommand({
    agentRuntimeArn,
    runtimeSessionId: sessionId,
    payload: Buffer.from(payload),
    qualifier: "DEFAULT"
  });

  const response = await client.send(command);

  const chunks = [];

  for await (const chunk of response.response) {
    chunks.push(
      Buffer.from(chunk).toString("utf-8")
    );
  }

  return chunks.join("");
}
13. Node.js REST API

Your backend can expose:

POST /api/chat

Example:

import express from "express";
import { askAgent } from "./agent.js";

const app = express();

app.use(express.json());

app.post("/api/chat", async (req, res) => {

  try {

    const userMessage = req.body.message;

    const response = await askAgent(userMessage);

    res.json({
      response
    });

  } catch (error) {

    console.error(error);

    res.status(500).json({
      error: "Unable to process request"
    });
  }
});

app.listen(3000);
14. Complete Node.js Flow
React
  │
  │ POST /api/chat
  ▼
Node.js
  │
  │ InvokeAgentRuntime
  │
  │ Runtime ARN
  │ + user message
  ▼
AgentCore Runtime
  │
  ▼
AI Agent

The response travels back:

AI Agent
   ↓
AgentCore Runtime
   ↓
Node.js
   ↓
React
   ↓
Customer
15. AgentCore Runtime ARN

After deploying your AgentCore Runtime, your application needs the Runtime identity/ARN.

Example:

arn:aws:bedrock-agentcore:
us-east-1:
123456789012:
runtime/ShoeAgent-abc123

Node.js stores it as server-side configuration:

AGENTCORE_RUNTIME_ARN=...

The application can invoke the Runtime using:

InvokeAgentRuntime
IAM Permission

The Node.js / Java application's AWS identity needs permission such as:

bedrock-agentcore:InvokeAgentRuntime

For example, the IAM role attached to:

EC2
ECS
EKS
Lambda

can be granted the required permission.

16. Agent Decides What to Do

Suppose the customer asks:

Show me running shoes under $100.

The Agent conceptually determines:

User wants products.

Possible sources:

1. Knowledge Base
2. Product-search tool

This is the difference between an Agent and a simple API.

Normal API
GET /products
Agent
User request
      ↓
Agent understands intent
      ↓
Decides what information is needed
      ↓
Chooses Knowledge Base / Tool
      ↓
Gets information
      ↓
Generates answer
17. RAG and Knowledge Base
What is RAG?

RAG = Retrieval-Augmented Generation

Instead of asking the LLM to rely only on its existing knowledge:

User question
      ↓
Retrieve relevant information
      ↓
Give information to LLM
      ↓
Generate answer
18. Knowledge Base Example

Suppose your shoe company has:

shoes/
├── nike-running.pdf
├── adidas-running.pdf
├── return-policy.pdf
├── size-guide.pdf
└── product-catalog.pdf

Upload these documents to:

Amazon S3

Then:

S3
 ↓
Knowledge Base
 ↓
Parse
 ↓
Chunk
 ↓
Embeddings
 ↓
Vector Store
Conceptual Ingestion Flow
19. How RAG Answers a Question

Customer:

What running shoe is good for flat feet?

Flow:

Agent
  ↓
Knowledge Base
  ↓
Vector Search
  ↓
Relevant Chunks
  ↓
Agent
  ↓
LLM
  ↓
Answer

The Knowledge Base retrieves relevant content.

The Agent then uses that information to generate the response.

20. Knowledge Base vs Gateway

This distinction is extremely important.

Requirement	Use
Product documentation	Knowledge Base
Return policy	Knowledge Base
Shipping policy	Knowledge Base
Size guide	Knowledge Base
Product descriptions	Knowledge Base
Current inventory	Gateway → Tool
Current order status	Gateway → Tool
Cancel order	Gateway → Tool
Create order	Gateway → Tool
Update customer data	Gateway → Tool
Easy Memory
Knowledge Base = KNOWLEDGE

Gateway = ACTION / TOOL ACCESS
21. Live Data vs Knowledge Data

Suppose your PDF says:

Nike Pegasus
Price: $89
Good for running

This is relatively static information.

But the customer asks:

Is Nike Pegasus size 10 available right now?

The PDF cannot reliably answer current inventory.

You need live backend data.

Therefore:

Agent
  ↓
Gateway
  ↓
Target
  ↓
Lambda
  ↓
RDS
22. Live Inventory Example

Agent calls:

check_inventory

Input:

{
  "product_id": "PEGASUS-2026",
  "size": 10
}

Gateway:

Agent
 ↓
Gateway
 ↓
Target
 ↓
cs_product_lambda

Lambda receives:

{
  "product_id": "PEGASUS-2026",
  "size": 10
}

Lambda gets credentials from Secrets Manager.

Then queries RDS:

SELECT quantity
FROM inventory
WHERE product_id = 'PEGASUS-2026'
AND size = 10;

RDS returns:

{
  "quantity": 7
}
23. Complete Shoe Shopping Flow

Customer asks:

Is Nike Pegasus size 10 available right now?

Complete architecture:

The Agent now knows:

Nike Pegasus
Size: 10
Price: $89
Stock: 7

It generates:

Nike Pegasus is available in size 10 for $89. There are currently 7 pairs in stock.

24. Complete Production Architecture
25. Route 53 vs API Gateway vs ALB

These services are easy to confuse.

Route 53
Question it answers:

WHERE should this domain go?

Example:

api.abcshoes.com
       ↓
AWS infrastructure

Think:

Route 53 = DNS
API Gateway
Question it answers:

WHICH API should handle this request?

Example:

POST /api/chat
       ↓
API Gateway
       ↓
Backend

Common responsibilities:

API routing
Authentication / authorization
Throttling
Request handling
API management

Think:

API Gateway = API Front Door
ALB
Question it answers:

WHICH healthy backend server/container should receive this request?

Example:

ALB
 ├── Node container 1
 ├── Node container 2
 └── Node container 3

Think:

ALB = Load Balancer
26. Web Layer vs AI Layer vs Tool/Data Layer

This is a very useful mental model.

Layer 1 — Web/Application
Customer
   ↓
React
   ↓
API Gateway / ALB
   ↓
Node.js / Java

Responsible for:

UI
Authentication
Application APIs
Business logic
Request handling
Layer 2 — AI
Node.js / Java
      ↓
AgentCore Runtime
      ↓
AI Agent

Responsible for:

Understanding user intent
Reasoning
Deciding what to do
Choosing tools
Generating responses
Layer 3 — Knowledge / Tools / Data
Knowledge
Agent
 ↓
Knowledge Base
 ↓
Vector Store
Tools
Agent
 ↓
Gateway
 ↓
Target
 ↓
Lambda
Data
Lambda
 ↓
Secrets Manager
 ↓
RDS
27. Two Different "Gateways"

Do not confuse these.

API Gateway
Customer
 ↓
API Gateway
 ↓
Node.js

Purpose:

HTTP API access
AgentCore Gateway
Agent
 ↓
AgentCore Gateway
 ↓
Tool / Target
 ↓
Lambda

Purpose:

AI Agent tool access
Easy Memory
API Gateway
     ↓
Human/Application → Backend

AgentCore Gateway
     ↓
Agent → Tools
28. Do We Always Need All Services?
No.

The architecture depends on your requirements.

Simple Agent

If you only need an Agent:

React
 ↓
Node.js
 ↓
AgentCore Runtime
 ↓
Agent

No Gateway required.

Agent + Documents

If the Agent needs company documents:

Node.js
 ↓
AgentCore Runtime
 ↓
Agent
 ↓
Knowledge Base
 ↓
Vector Store
Agent + Live Backend Data

If the Agent needs live data:

Node.js
 ↓
AgentCore Runtime
 ↓
Agent
 ↓
Gateway
 ↓
Target
 ↓
Lambda
 ↓
RDS
Full Production Agent
Customer
 ↓
React
 ↓
API Gateway / ALB
 ↓
Node.js
 ↓
AgentCore Runtime
 ↓
Agent
 ├── Knowledge Base
 └── Gateway
      ↓
     Target
      ↓
    Lambda
      ├── Secrets Manager
      └── RDS
29. Security Boundaries

A very important principle:

Browser should know only what it needs.

React should NOT contain:

❌ AWS access keys
❌ DB username
❌ DB password
❌ RDS endpoint
❌ Secrets Manager secret
❌ Lambda credentials
❌ Internal Gateway configuration
Backend

Node.js / Java can know:

AgentCore Runtime ARN
AWS Region

and use its IAM role to invoke AgentCore.

Lambda

Lambda can know:

Secret ARN

and use IAM permission to retrieve the actual credentials.

Secrets Manager

Stores:

DB username
DB password
DB host
DB port
RDS

Stores:

Orders
Products
Inventory
Customers
30. Final Security Flow
React
  │
  │ User message
  ▼
Node.js
  │
  │ IAM
  ▼
AgentCore Runtime
  │
  ▼
Agent
  │
  │ Tool request
  ▼
Gateway
  │
  ▼
Lambda
  │
  │ IAM
  ▼
Secrets Manager
  │
  │ Credentials
  ▼
Lambda
  │
  ▼
RDS
Critical Principle
Agent gets BUSINESS DATA

Lambda gets DB CREDENTIALS

Secrets Manager stores CREDENTIALS

RDS stores DATABASE DATA
31. One-Line Architecture

The complete architecture can be remembered as:

Customer
 → React
 → Node/Java
 → AgentCore Runtime
 → Agent
 → Knowledge Base OR Gateway
 → Target
 → Lambda
 → Secrets Manager / RDS
 → Agent
 → Node/Java
 → React
 → Customer
32. Service Mental Model
AWS / Application Component	Think About It As
Route 53	WHERE?
CloudFront	DELIVER?
API Gateway	WHICH API?
ALB	WHICH SERVER?
Node.js / Java	BUSINESS LOGIC
AgentCore Runtime	RUN AGENT
Agent	THINK / DECIDE
Knowledge Base	KNOWLEDGE
RAG	RETRIEVE KNOWLEDGE
AgentCore Gateway	TOOL ACCESS
Gateway Target	WHERE TOOL GOES
Lambda	EXECUTE
Secrets Manager	CREDENTIALS
RDS	DATA
33. The Most Important Flows to Memorize
Flow A — Simple Agent
Frontend
   ↓
Backend
   ↓
AgentCore Runtime
   ↓
Agent
   ↓
Response
Flow B — Agent + RAG
Frontend
   ↓
Backend
   ↓
AgentCore Runtime
   ↓
Agent
   ↓
Knowledge Base
   ↓
Vector Search
   ↓
Relevant Chunks
   ↓
Agent
   ↓
Response
Flow C — Agent + Live Database
Frontend
   ↓
Backend
   ↓
AgentCore Runtime
   ↓
Agent
   ↓
Gateway
   ↓
Target
   ↓
Lambda
   ↓
Secrets Manager
   ↓
RDS
   ↓
Lambda
   ↓
Gateway
   ↓
Agent
   ↓
Backend
   ↓
Frontend
Flow D — Agent + RAG + Live Data
                       ┌── Knowledge Base
                       │
                       │
AgentCore Runtime → Agent
                       │
                       │
                       └── Gateway
                              ↓
                            Target
                              ↓
                            Lambda
                           ↙      ↘
                  Secrets Manager  RDS

This is the core production mental model.

34. Developer Perspective — What Do I Actually Build?

If you're a Node.js / Java developer, your responsibilities might look like this:

Frontend
React / Angular

Send:

{
  "message": "Is Nike Pegasus size 10 available?"
}
Backend

Create:

POST /api/chat

Then invoke:

InvokeAgentRuntime

using:

AgentCore Runtime ARN
AgentCore

Deploy:

AI Agent

Configure its behavior and capabilities.

Knowledge Base

Configure:

S3
 ↓
Knowledge Base
 ↓
Chunking
 ↓
Embeddings
 ↓
Vector Store
Gateway

Configure tools:

check_inventory
get_order_details
cancel_order
search_products
Target

Map tools to backend implementations:

check_inventory
       ↓
Lambda
Lambda

Perform backend operations:

Lambda
 ↓
Secrets Manager
 ↓
RDS
35. Important Distinctions
AgentCore Runtime vs AgentCore Gateway
Runtime
  =
Where the Agent runs
Gateway
  =
How the Agent accesses tools
Knowledge Base vs RDS
Knowledge Base
  =
Unstructured / reference knowledge

Examples:

PDF
Policies
Documentation
Product descriptions
FAQs
RDS
  =
Transactional / structured application data

Examples:

Orders
Inventory
Customers
Payments
Secrets Manager vs RDS
Secrets Manager
  =
Credentials
RDS
  =
Database data
API Gateway vs AgentCore Gateway
API Gateway
  =
Application API front door
AgentCore Gateway
  =
Agent tool access layer
36. Quick Revision 🚀

Before an exam/interview, remember these questions.

Where does the Agent run?
AgentCore Runtime
How does Node.js invoke the Agent?
InvokeAgentRuntime
What identifies the deployed Runtime?
Runtime ARN
Does React directly need the Runtime ARN?
NO
Why?
Security + backend abstraction
When does Agent use Gateway?
When it needs external tools/backend capabilities
What does Gateway connect to?
Targets
What can a Target point to?
Backend capability such as Lambda
Where should DB credentials live?
Secrets Manager
Who retrieves DB credentials?
Lambda
Who queries RDS?
Lambda
Does Agent receive DB password?
NO
Where should static company documents go?
S3 → Knowledge Base
What does RAG do?
Retrieve relevant knowledge
        ↓
Give it to the Agent / LLM
        ↓
Generate answer
Where does live inventory come from?
Gateway → Target → Lambda → RDS
37. Easy Memory 🧠

Memorize this:

ROUTE 53
    ↓
"WHERE?"

CLOUDFRONT
    ↓
"DELIVER"

API GATEWAY
    ↓
"WHICH API?"

ALB
    ↓
"WHICH SERVER?"

NODE / JAVA
    ↓
"BUSINESS LOGIC"

AGENTCORE RUNTIME
    ↓
"RUN THE AGENT"

AGENT
    ↓
"THINK / DECIDE"

KNOWLEDGE BASE
    ↓
"KNOW"

GATEWAY
    ↓
"GET TOOLS"

TARGET
    ↓
"WHERE DOES TOOL GO?"

LAMBDA
    ↓
"EXECUTE"

SECRETS MANAGER
    ↓
"GET CREDENTIALS"

RDS
    ↓
"GET DATA"
38. Final Interview Answer

If an interviewer asks:

How would you integrate an AI Agent with an existing Node.js application and database?

A strong answer:

The React frontend sends the user's request to our Node.js backend.

The Node.js backend invokes the deployed Amazon Bedrock AgentCore
Runtime using InvokeAgentRuntime and the Runtime ARN.

The Agent analyzes the request and decides whether it needs
knowledge from a Bedrock Knowledge Base or needs to invoke a backend
tool through AgentCore Gateway.

For knowledge-based questions, the Agent retrieves relevant
information from the Knowledge Base using RAG.

For live transactional data such as inventory or order status,
the Agent invokes a tool exposed through AgentCore Gateway.

The Gateway routes the request to the configured target, such as
a Lambda function.

The Lambda retrieves database credentials from AWS Secrets Manager,
queries RDS, and returns only the required business data.

The Agent uses that result to generate the final response, which
flows back through AgentCore Runtime to Node.js and finally to
the React frontend.

Database credentials never need to be exposed to the Agent or
frontend.
⭐ Final Mental Model
                CUSTOMER
                    │
                    ▼
              React / Angular
                    │
                    ▼
             Node.js / Java
                    │
                    ▼
          ┌───────────────────┐
          │ AgentCore Runtime │
          │                   │
          │       AGENT       │
          └─────────┬─────────┘
                    │
             ┌──────┴──────┐
             │             │
             ▼             ▼
       Knowledge Base    Gateway
             │             │
             │           Target
             │             │
             │          Lambda
             │          /     \
             │         /       \
             │  Secrets Manager RDS
             │
             ▼
          Knowledge
             │
             └──────────────┐
                            ▼
                          Agent
                            │
                            ▼
                       Node / Java
                            │
                            ▼
                           UI
                            │
                            ▼
                         Customer
🔑 The 5 Things to Never Forget
1. Runtime = RUN the Agent

2. Agent = THINK / DECIDE

3. Knowledge Base = KNOWLEDGE / RAG

4. Gateway = TOOL ACCESS

5. Lambda + Secrets Manager + RDS = SECURE BACKEND DATA ACCESS

Core sentence to remember:

Node/Java invokes AgentCore Runtime → Agent decides → Knowledge Base provides knowledge OR Gateway provides tools → Lambda executes backend operations → Secrets Manager protects credentials → RDS provides live data → Agent generates the final answer.